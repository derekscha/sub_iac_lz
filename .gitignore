# Editor / OS artifacts
null
.DS_Store
Thumbs.db
.vscode/*
!.vscode/extensions.json

# Azure CLI / Bicep local state
.azure/
*.bicep.json

# Terraform (state bootstrapped by this repo, not committed)
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
