# scripts

Helper scripts that wrap deployment and validation commands (`az deployment group create`,
`az bicep lint`, pre-commit formatting) so they run identically on a developer machine and in CI/CD.
