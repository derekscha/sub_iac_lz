# tests

Validation for the Bicep templates in this repo — `az bicep build`/`lint` checks and
what-if / Pester-based deployment tests. Mirror the folder name of the workload under test
(e.g. `tests/terraform-bootstrap/`).
